InsertionSort(A)
for i = 1 to length(A) - 1 do   //Iterate over the array starting from the second element
j = i   //Store the current index in a variable
while j > 0 and A[j] < A[j-1] do   //Continue swapping elements until the current element is in its correct position
swap A[j] and A[j-1]   //Swap the current element with the previous element
j = j - 1   //Move the index to the left
end while
end for
